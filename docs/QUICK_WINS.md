# Quick Wins

1. Add `await` to every `github.rest.issues.*` mutation call.
2. Replace multi-step nightly flow with single script block.
3. Add pagination utility (`page < 10` guard + iterator).
4. Add `concurrency` to each workflow.
5. Add idempotency: check existing report by date before create.
6. Add actionlint workflow.
